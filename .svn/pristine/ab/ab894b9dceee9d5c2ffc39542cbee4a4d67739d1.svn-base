package group9.unit_test;

import group9.utils.Utils;
import org.junit.Assert;
import org.junit.Test;

import javax.imageio.ImageIO;
import java.awt.image.BufferedImage;
import java.io.File;
import java.io.IOException;

public class ImageRescalingTest {

    @Test
    public  void testRescaling() throws IOException {
        File kitten = new File("trunk\\resources\\testImages\\kitten.jpg");
        File testKitten = new File("trunk\\resources\\testImages\\testkitten.jpg");
        BufferedImage kittenImage = ImageIO.read(kitten);
        BufferedImage kittenImageRescaled = ImageIO.read(testKitten);
        int newWidth = 600;
        int newHeight = 600;
        int originalWidth = kittenImage.getWidth();
        int originalHeight = kittenImage.getHeight();
        int rescaledWidth = kittenImageRescaled.getWidth();
        int rescaledHeight = kittenImageRescaled.getHeight();
        Utils.rescaleImage("jpg",kitten,testKitten,newWidth,newHeight);
        Assert.assertEquals(newWidth,rescaledWidth);
        Assert.assertEquals(newHeight,rescaledHeight);
    }
}
